import { NotaryServiceFirstStep } from './notary-service-first-step';

describe('NotaryServiceFirstStep', () => {
  it('should create an instance', () => {
    expect(new NotaryServiceFirstStep()).toBeTruthy();
  });
});
