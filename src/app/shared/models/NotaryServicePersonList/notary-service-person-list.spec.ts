import { NotaryServicePersonList } from './notary-service-person-list';

describe('NotaryServicePersonList', () => {
  it('should create an instance', () => {
    expect(new NotaryServicePersonList()).toBeTruthy();
  });
});
